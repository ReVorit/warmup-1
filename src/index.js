module.exports = function warmup(temperature) {
 
  var warmup;

    var temperature

        temperature *=9;
        temperature /=5;
        temperature +=32;

        warmup = temperature;

    return warmup;
};
